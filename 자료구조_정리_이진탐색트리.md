---

# **📚 이진 탐색 트리 (BST) 개념 정리북**  

## **🌳 1. 이진 탐색 트리란?**  

이진 탐색 트리(Binary Search Tree, BST)는 **이진 트리**의 한 종류로, 데이터가 **정렬된 상태로 저장**되며 효율적인 탐색이 가능합니다.  

- 각 노드는 **최대 2개의 자식 노드**를 가짐  
- **왼쪽 자식** → 부모보다 **작은 값**  
- **오른쪽 자식** → 부모보다 **큰 값**  
- 평균 탐색 시간복잡도 → **O(log n)**  
- 최악의 경우(편향 트리) → **O(n)**  
- **이진 탐색**이 가능해 탐색 속도가 빠름  

---

## **✍️ 2. 삽입 과정**  

### **삽입 예시 문제**  
삽입 순서: `10 → 5 → 15 → 3 → 7 → 12 → 18`  

트리 구조:  

```
       10
      /  \
     5    15
    / \   /  \
   3   7 12  18
```

### **삽입 과정 코드**  

```cpp
struct TreeNode {
    int data;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int val) : data(val), left(nullptr), right(nullptr) {}
};

TreeNode* insert(TreeNode* root, int value) {
    if (!root) return new TreeNode(value);
    if (value < root->data) root->left = insert(root->left, value);
    else if (value > root->data) root->right = insert(root->right, value);
    return root;
}
```

---

## **🧹 3. 삭제 과정**  

### **Case 1️⃣: 리프 노드 삭제**  
→ 그냥 삭제  

### **Case 2️⃣: 자식이 하나인 노드 삭제**  
→ 자식을 대신 연결  

### **Case 3️⃣: 자식이 두 개인 노드 삭제**  
→ **오른쪽 서브트리에서 최솟값을 찾아 대체 후 재귀 삭제**  

### **삭제 예시: 노드 70 삭제**  

```
    50
   /  \
  30   70
       /  \
      60   90
          /
        80
          \
          85
```

**삭제 과정:**  
1️⃣ 오른쪽 서브트리에서 **최솟값(80)** 찾기  
2️⃣ 노드 70 → **80으로 교체**  
3️⃣ 기존 80(리프 노드) 삭제  

삭제 후 트리 구조:  

```
    50
   /  \
  30   80
       /  \
      60   90
            \
            85
```

### **삭제 과정 코드**  

```cpp
TreeNode* findMin(TreeNode* node) {
    while (node && node->left) node = node->left;
    return node;
}

TreeNode* deleteNode(TreeNode* root, int value) {
    if (!root) return nullptr;

    if (value < root->data) root->left = deleteNode(root->left, value);
    else if (value > root->data) root->right = deleteNode(root->right, value);
    else {
        if (!root->left && !root->right) {
            delete root;
            return nullptr;
        } else if (!root->left) {
            TreeNode* temp = root->right;
            delete root;
            return temp;
        } else if (!root->right) {
            TreeNode* temp = root->left;
            delete root;
            return temp;
        }
        TreeNode* temp = findMin(root->right);
        root->data = temp->data;
        root->right = deleteNode(root->right, temp->data);
    }

    return root;
}
```

---

## **🔍 4. 탐색 및 순회**  

### **탐색 코드**  

```cpp
bool search(TreeNode* root, int value) {
    if (!root) return false;
    if (root->data == value) return true;
    return (value < root->data) ? search(root->left, value) : search(root->right, value);
}
```

### **중위 순회 코드**  

```cpp
void inorder(TreeNode* root) {
    if (!root) return;
    inorder(root->left);
    cout << root->data << " ";
    inorder(root->right);
}
```

---

## **🎈 5. 실생활 활용 사례**  


---

## **📂 파일 시스템 탐색기 (BST 기반)**  

### **1️⃣ 파일 삽입 코드**  
새로운 파일(문서)을 추가하는 기능입니다.  

```cpp
struct FileNode {
    std::string filename;
    FileNode* left;
    FileNode* right;
    FileNode(std::string name) : filename(name), left(nullptr), right(nullptr) {}
};

FileNode* insert(FileNode* root, std::string filename) {
    if (!root) return new FileNode(filename);
    if (filename < root->filename) root->left = insert(root->left, filename);
    else if (filename > root->filename) root->right = insert(root->right, filename);
    return root;
}
```

---

### **2️⃣ 파일 탐색 코드**  
사용자가 원하는 파일이 있는지 탐색하는 기능입니다.  

```cpp
bool search(FileNode* root, std::string filename) {
    if (!root) return false;
    if (root->filename == filename) return true;
    return (filename < root->filename) ? search(root->left, filename) : search(root->right, filename);
}
```

---

### **3️⃣ 파일 삭제 코드**  
파일을 삭제할 때 BST 구조를 유지하며 정리합니다.  

```cpp
FileNode* deleteFile(FileNode* root, std::string filename) {
    if (!root) return nullptr;

    if (filename < root->filename) root->left = deleteFile(root->left, filename);
    else if (filename > root->filename) root->right = deleteFile(root->right, filename);
    else {
        if (!root->left) return root->right;
        if (!root->right) return root->left;
        FileNode* temp = root->right;
        while (temp->left) temp = temp->left;
        root->filename = temp->filename;
        root->right = deleteFile(root->right, temp->filename);
    }
    return root;
}
```

---

### **4️⃣ 파일 목록 출력 (중위 순회 활용)**  
파일명을 **오름차순으로 정렬**하여 출력하는 코드입니다.  

```cpp
void listFiles(FileNode* root) {
    if (!root) return;
    listFiles(root->left);
    std::cout << root->filename << std::endl;
    listFiles(root->right);
}
```

---

# 요약버전 
---

### 📜 **압축 요약 버전**  

📚 **이진 탐색 트리(BST)**  
- **이진 트리**의 한 종류, 정렬된 구조 유지  
- **왼쪽 자식** → 작은 값, **오른쪽 자식** → 큰 값  
- **탐색 시간복잡도**: 평균 O(log n), 최악 O(n)  

✍️ **삽입**  
삽입 순서 → `10, 5, 15, 3, 7, 12, 18`  
트리 구조  
```
       10
      /  \
     5    15
    / \   /  \
   3   7 12  18
```
**코드 (핵심 부분만)**  
```cpp
TreeNode* insert(TreeNode* root, int value) {
    if (!root) return new TreeNode(value);
    if (value < root->data) root->left = insert(root->left, value);
    else root->right = insert(root->right, value);
    return root;
}
```

🧹 **삭제**  
- **리프 노드** → 그냥 삭제  
- **자식 하나** → 부모와 직접 연결  
- **자식 둘** → 오른쪽 서브트리 최솟값을 가져와 대체  

🔍 **탐색**  
파일을 찾고자 할 때 BST 구조 활용  
```cpp
bool search(TreeNode* root, int value) {
    if (!root) return false;
    return (value < root->data) ? search(root->left, value) : search(root->right, value);
}
```

🎈 **실생활 적용: 파일 탐색기(File Explorer)**  
- 파일명 기준 BST 적용 → 정렬 및 빠른 검색  
- Windows 탐색기, Mac Finder 등 활용  
- 탐색 예제 → `"Project_"`로 시작하는 파일 찾기  

```cpp
void listFiles(FileNode* root) {
    if (!root) return;
    listFiles(root->left);
    std::cout << root->filename << std::endl;
    listFiles(root->right);
}
```

---

