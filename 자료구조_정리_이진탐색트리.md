---

# **📚 이진 탐색 트리 (BST) 개념 정리북**  

## **🌳 1. 이진 탐색 트리란?**  

이진 탐색 트리(Binary Search Tree, BST)는 **이진 트리**의 한 종류로, 데이터가 **정렬된 상태로 저장**되며 효율적인 탐색이 가능합니다.  

- 각 노드는 **최대 2개의 자식 노드**를 가짐  
- **왼쪽 자식** → 부모보다 **작은 값**  
- **오른쪽 자식** → 부모보다 **큰 값**  
- 평균 탐색 시간복잡도 → **O(log n)**  
- 최악의 경우(편향 트리) → **O(n)**  
- **이진 탐색**이 가능해 탐색 속도가 빠름  

---

## **✍️ 2. 삽입 과정**  

### **삽입 예시 문제**  
삽입 순서: `10 → 5 → 15 → 3 → 7 → 12 → 18`  

트리 구조:  

```
       10
      /  \
     5    15
    / \   /  \
   3   7 12  18
```

### **삽입 과정 코드**  

```cpp
struct TreeNode {
    int data;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int val) : data(val), left(nullptr), right(nullptr) {}
};

TreeNode* insert(TreeNode* root, int value) {
    if (!root) return new TreeNode(value);
    if (value < root->data) root->left = insert(root->left, value);
    else if (value > root->data) root->right = insert(root->right, value);
    return root;
}
```

---

## **🧹 3. 삭제 과정**  

### **Case 1️⃣: 리프 노드 삭제**  
→ 그냥 삭제  

### **Case 2️⃣: 자식이 하나인 노드 삭제**  
→ 자식을 대신 연결  

### **Case 3️⃣: 자식이 두 개인 노드 삭제**  
→ **오른쪽 서브트리에서 최솟값을 찾아 대체 후 재귀 삭제**  

### **삭제 예시: 노드 70 삭제**  

```
    50
   /  \
  30   70
       /  \
      60   90
          /
        80
          \
          85
```

**삭제 과정:**  
1️⃣ 오른쪽 서브트리에서 **최솟값(80)** 찾기  
2️⃣ 노드 70 → **80으로 교체**  
3️⃣ 기존 80(리프 노드) 삭제  

삭제 후 트리 구조:  

```
    50
   /  \
  30   80
       /  \
      60   90
            \
            85
```

### **삭제 과정 코드**  

```cpp
TreeNode* findMin(TreeNode* node) {
    while (node && node->left) node = node->left;
    return node;
}

TreeNode* deleteNode(TreeNode* root, int value) {
    if (!root) return nullptr;

    if (value < root->data) root->left = deleteNode(root->left, value);
    else if (value > root->data) root->right = deleteNode(root->right, value);
    else {
        if (!root->left && !root->right) {
            delete root;
            return nullptr;
        } else if (!root->left) {
            TreeNode* temp = root->right;
            delete root;
            return temp;
        } else if (!root->right) {
            TreeNode* temp = root->left;
            delete root;
            return temp;
        }
        TreeNode* temp = findMin(root->right);
        root->data = temp->data;
        root->right = deleteNode(root->right, temp->data);
    }

    return root;
}
```

---

## **🔍 4. 탐색 및 순회**  

### **탐색 코드**  

```cpp
bool search(TreeNode* root, int value) {
    if (!root) return false;
    if (root->data == value) return true;
    return (value < root->data) ? search(root->left, value) : search(root->right, value);
}
```

### **중위 순회 코드**  

```cpp
void inorder(TreeNode* root) {
    if (!root) return;
    inorder(root->left);
    cout << root->data << " ";
    inorder(root->right);
}
```

---

## **🎈 5. 실생활 활용 사례**  

**📁 파일 시스템 탐색기 (File Explorer)**  

### **활용 사례**  
Windows 탐색기, Mac Finder, Linux Nautilus 등의 **파일 시스템**에서 **BST 구조를 이용**하여 파일과 폴더를 효율적으로 탐색하고 정렬된 상태로 보여줍니다.  

### **적용 이유**  
- 파일명으로 탐색 시 → **O(log n) 속도** 가능  
- 파일 추가/삭제 시 BST 구조 유지  
- **왼쪽 서브트리** → 이름(혹은 시간 기준)으로 더 작은 것  
- **오른쪽 서브트리** → 더 큰 것  

### **구체적 예시**  
📂 **루트 디렉터리**: `Documents`  

📁 **하위 폴더 (BST 구조)**  
- `2021_Report`  
- `2022_Report`  
- `Project_Alpha`  
- `Project_Beta`  

➡ 탐색기에서 `"Project_"`로 시작하는 파일을 찾을 때 → **BST 탐색**  

➡ **삽입**: 새로운 문서 추가  
➡ **삭제**: 오래된 문서 삭제  

---

